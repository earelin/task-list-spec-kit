micronaut:
  application:
    name: tasklist-backend
    version: ${PROJECT_VERSION:1.0.0}
  server:
    port: ${MICRONAUT_SERVER_PORT:8080}

endpoints:
  health:
    enabled: false
  info:
    enabled: true
    sensitive: false
